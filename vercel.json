{
  "version": 2,
  "builds": [
    {
      "src": "server/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["client/**", "server/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/chat(.*)",
      "dest": "server/server.js"
    },
    {
      "src": "/upload(.*)",
      "dest": "server/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "server/server.js"
    }
  ]
}
